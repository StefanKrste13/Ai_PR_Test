name: AI PR Review (Ollama)

on:
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  review:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get diff (trimmed)
        run: |
          git fetch origin ${{ github.event.pull_request.base.ref }}
          git diff origin/${{ github.event.pull_request.base.ref }}...HEAD \
          | head -c 12000 > diff.txt

      - name: Create payload
        run: |
          python3 - << 'EOF'
          import json

          diff = open("diff.txt", "r", errors="ignore").read()

          payload = {
              "model": "qwen3.5:35b",
              "stream": False,
              "options": {
                  "num_predict": 1024,
                  "temperature": 0.2
              },
              "prompt": (
                  "You are a senior software engineer reviewing a pull request.\n"
                  "Return ONLY a clean readable PR review.\n"
                  "DO NOT include thinking process or JSON.\n\n"
                  "Format:\n"
                  "- Bugs\n"
                  "- Security issues\n"
                  "- Performance issues\n"
                  "- Improvements\n"
                  "- Summary\n\n"
                  "DIFF:\n"
              ) + diff
          }

          with open("payload.json", "w") as f:
              json.dump(payload, f)
          EOF

      - name: Call Ollama via ngrok
        run: |
          curl -s https://budget-gracious-boozy.ngrok-free.dev/api/generate \
          -H "Content-Type: application/json" \
          -d @payload.json > response.json

      - name: Debug response
        run: cat response.json

      - name: Post comment (FIXED CLEAN OUTPUT)
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');

            const raw = fs.readFileSync('response.json', 'utf8');

            let text = "";

            try {
              const data = JSON.parse(raw);
              text = data.response || "";
            } catch (e) {
              text = raw;
            }

            text = text
              .replace(/\\n/g, "\n")
              .replace(/\\r/g, "")
              .trim();

            if (!text || text.length < 10) {
              text = "❌ Empty AI response (model truncated or failed)";
            }

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: "## 🤖 AI PR Review\n\n" + text
            });